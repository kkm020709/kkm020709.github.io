---
import ArchivePanel from "@/components/controls/ArchivePanel.svelte";

const { page } = Astro.props;
---

<ArchivePanel
  sortedPosts={page.data}
  panelTitle="文章"
  enableFilters={false}
  client:only="svelte"
/>
